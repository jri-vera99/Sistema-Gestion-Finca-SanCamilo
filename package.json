{
  "name": "sistema-gestion-finca-sancamilo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18"
  }
}
